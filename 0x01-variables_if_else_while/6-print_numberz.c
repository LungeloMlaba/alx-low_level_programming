#include <stdio.h>
/**
 * main - prints numbers from base 10.
 *
 * Return: Always 0 (success)
 */

int main(void)
{
        int q;

        q = 0;
        while
        (q < 10) {
                putchar(q + '0');
                        q++;
        }
putchar('\n');
return (0);
}
