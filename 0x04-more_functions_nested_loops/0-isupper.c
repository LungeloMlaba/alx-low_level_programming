#include "main.h"

/**
 * _isupper - Checks wheather letter is in uppercase
 * @q: The num to be checked
 * Return: 1 for upper letter and 0 for anything else
 */
int _isupper(int q)
{
	if (q >= 65 && q <= 90)
	{
	return (1);
	}
	return (0);
}
